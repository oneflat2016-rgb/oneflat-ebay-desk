/**
 * TODO(§53): 実装対象。eBay Fulfillment API。
 * 注文情報の取得・同期(Phase2)。POST /api/ebay/sync/orders から呼ばれる。
 */
export async function syncOrders(): Promise<void> {
  throw new Error('syncOrders is Phase2 scope (§53), not implemented yet');
}
