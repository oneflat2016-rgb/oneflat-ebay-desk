/**
 * TODO(§53): 実装対象。eBay Finances API。
 * 売上・各種販売費用・返金等の取引情報の取得・同期(Phase2)。
 * POST /api/ebay/sync/finances から呼ばれる。
 */
export async function syncFinances(): Promise<void> {
  throw new Error('syncFinances is Phase2 scope (§53), not implemented yet');
}
